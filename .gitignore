# --- Source data (too large for GitHub; reproduce locally) ---
data.csv
metadata.csv
data_preprocessed.csv

# --- Evaluation data ---
covidfact.jsonl
evaluation_results.csv
evaluation_summary.txt

# NOTE: the prebuilt embedding index files (sentence_index.csv,
# sentence_embeddings.npy, doc_embeddings.npy, index_config.json) are
# INTENTIONALLY tracked so Streamlit Cloud can load them at startup.
# They total ~100 MB and fit within GitHub's per-file limits.

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/

# --- Virtual environments ---
venv/
.venv/
env/
.env

# --- Jupyter ---
.ipynb_checkpoints/

# --- Streamlit ---
.streamlit/secrets.toml

# --- Editor / OS ---
.DS_Store
.vscode/
.idea/
*.swp
*~

# --- Model caches (HuggingFace downloads) ---
.cache/
~/.cache/huggingface/
